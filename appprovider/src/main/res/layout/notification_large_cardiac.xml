<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <LinearLayout
        android:id="@+id/request_immediate_card_view"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:background="@color/cardiac_bg_color"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:divider="@android:color/transparent"
        android:dividerHeight="0dp"
        tools:ignore="SpUsage">

        <TextView
            android:id="@+id/title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="large layout large layout large layout large layout layout large layout large layout large layout layout large layout large layout large layout layout large layout large layout large layout layout large layout large layout large layout"
            android:textSize="20dp" />
    </LinearLayout>
</layout>